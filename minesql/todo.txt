table:
    create: 后面的句子解析，判错
        foreign key: 创建的时候查询是否正确
    insert: 增加数据的时候注意foreign key, 还有select之后的 违反需要报出具体的错误
    delete:
    update: 增加数据的时候注意 primary key, foreign key, check, unique, not null/null 还有select之后的 违反需要报出具体的错误
    select:
        单表 连接 嵌套 集合
        where支持 or between and in
index:
    create:

view:
    create:


help:
    database: 输出所有数据表、视图和索引的信息，同时显示其对象类型
    view + 视图名: 输出视图的定义语句
    index + 索引名: 输出索引的详细信息